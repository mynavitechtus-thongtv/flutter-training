# Code Walk — Architecture & Barrel Files

> 📌 **Recap từ modules trước:**
> - **M0:** `pubspec.yaml` (dependency pinning, annotation ↔ generator pairs), `make sync` / `make ep` / `make fb` (automation commands), codegen pipeline ([M0 § makefile](../module-00-dart-primer/01-code-walk.md#makefile--automation-shortcuts))
> - **M1:** `main()` → `AppInitializer.init()` → `configureInjection()` → `runApp(ProviderScope(MyApp))` (boot sequence), `runZonedGuarded` (global error boundary), DI setup (`get_it` + `injectable`) ([M1 § di.dart](../module-01-app-entrypoint/01-code-walk.md#didart--dependency-injection-setup))
>
> Nếu chưa nắm vững → quay lại [Module 0](../module-00-dart-primer/) hoặc [Module 1](../module-01-app-entrypoint/) trước.

---

## index.dart — Barrel File của toàn bộ project

<!-- AI_VERIFY: base_flutter/lib/index.dart#L1-L3 -->
```dart
/// GENERATED CODE - DO NOT MODIFY BY HAND
/// Generated by: dart tools/dart_tools/lib/export_all_files.dart

export 'app_initializer.dart';
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/index.dart)

> 🔎 **Quan sát**
> - Dòng 1-2: comment `GENERATED CODE` — file này **không được sửa tay**, auto-generated bởi tool
> - Generator: `dart tools/dart_tools/lib/export_all_files.dart` — Dart script quét `lib/` và tạo export statements
> - Chạy qua `make ep` (đã học ở [M0](../module-00-dart-primer/01-code-walk.md#makefile--automation-shortcuts))
> - **Hỏi:** Tại sao file này chỉ dùng `export` mà không có bất kỳ `import` nào?

> 💡 **FE Perspective**
> **Flutter:** `index.dart` barrel file dùng `export 'file.dart'` re-export toàn bộ public symbols trong file — mọi thứ public tự động available, chỉ `_private` prefix bị ẩn.
> **React/Vue tương đương:** `index.ts` barrel file dùng `export { Named } from './file'` — phải named export từng symbol.
> **Khác biệt quan trọng:** Dart barrel export **toàn bộ** public symbols tự động (đơn giản hơn), còn JS/TS phải liệt kê từng named export (kiểm soát tốt hơn nhưng verbose hơn).

---

### Layer structure qua index.dart exports

> Đọc nhanh — chú ý **nhóm layer** (comment) chứ không cần nhớ từng dòng.

<!-- AI_VERIFY: base_flutter/lib/index.dart#L4-L30 -->
```dart
// ── Root ──
export 'app_initializer.dart';

// ── common/ — config, helpers, types, utils ──
export 'common/config.dart';
export 'common/helper/analytics/analytics_helper.dart';
export 'common/type/result.dart';
export 'common/util/log.dart';
// ... (config, constant, env, 11 helpers, 2 hooks, 3 types, 7 utils)

// ── data_source/ — API, database, Firebase, preferences ──
export 'data_source/api/app_api_service.dart';
export 'data_source/api/client/base/rest_api_client.dart';
export 'data_source/api/middleware/access_token_interceptor.dart';
// ... (API clients, JSON decoders, interceptors, database, Firebase)

// ── exception/, model/, navigation/, resource/, ui/ ──
export 'exception/app_exception.dart';
export 'navigation/app_navigator.dart';
export 'resource/app_colors.dart';
export 'ui/base/base_page.dart';
export 'ui/page/home/home_page.dart';
// ... (remaining layers follow same pattern)
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/index.dart)

> 📦 **Pattern chung**: Mỗi layer có exports sắp xếp theo **thứ tự alphabet** theo path. **162 lines** exports = toàn bộ public API qua **1 file duy nhất**.
>
> | Layer | Ví dụ exports | ~Số files |
> |-------|--------------|:---------:|
> | `common/` | config, helpers, hooks, types, utils | ~25 |
> | `data_source/` | API clients, decoders, interceptors, database, Firebase, preferences | ~36 |
> | `exception/` | AppException, mappers, handlers | ~9 |
> | `model/` | API responses, entities, enums, converters | ~5 |
> | `navigation/` | AppRouter, RouteGuard, observer | ~5 |
> | `resource/` | colors, fonts, images, shadows, themes | ~9 |
> | `ui/` | base classes, components, pages, popups, shared | ~40 |

> 🔎 **Quan sát**
> - Exports sắp xếp theo **thứ tự alphabet** — convention giúp dễ tìm kiếm
> - `common/` xuất hiện đầu tiên — layer nền tảng, module nào cũng dùng
> - `data_source/` chia sub-layers: `api/`, `database/`, `firebase/`, `preference/`
> - `ui/` chia: `base/` (abstract classes) → `component/` (reusable) → `page/` (page modules) → `popup/` → `shared/`
> - **Pattern page structure:** `ui/page/{name}/{name}_page.dart` + `view_model/{name}_state.dart` + `view_model/{name}_view_model.dart`
> - `export 'di.dart'` và `export 'main.dart'` cũng nằm trong barrel — **tất cả** public files đều export

> 📖 Full file: xem trực tiếp tại [`lib/index.dart`](../../base_flutter/lib/index.dart)

---

## Project Layer Architecture — Tổng quan

Từ `index.dart` exports, ta rút ra **7 layers** của project:

```
lib/
├── common/          ← Config, helpers, hooks, types, utils (foundation)
├── data_source/     ← API clients, database, Firebase, preferences (data)
├── exception/       ← Custom exceptions, mappers, handlers (error handling)
├── model/           ← API models, entities, enums, converters (data models)
├── navigation/      ← Router, guards, observers (routing)
├── resource/        ← Colors, fonts, images, themes (design tokens)
├── ui/              ← Pages, components, popups, base classes (presentation)
├── generated/       ← Codegen output (auto-generated)
├── index.dart       ← Barrel file (auto-generated)
├── di.dart          ← DI setup
├── di.config.dart   ← DI generated config
├── main.dart        ← Entry point
└── app_initializer.dart
```

> 💡 **FE Perspective**
> **Flutter:** Project tổ chức 7 layers theo trách nhiệm: `common/`, `data_source/`, `exception/`, `model/`, `navigation/`, `resource/`, `ui/` — mỗi layer có folder riêng, dependency direction top→bottom.
> **React/Vue tương đương:** `utils/` + `hooks/` ≈ `common/`, `services/` + `api/` ≈ `data_source/`, `types/` ≈ `model/`, `routes/` ≈ `navigation/`, `styles/` ≈ `resource/`, `components/` + `pages/` ≈ `ui/`.
> **Khác biệt quan trọng:** Flutter tách riêng `model/`, `exception/`, `resource/` thành layer độc lập — FE thường gom chung vào `utils/` hoặc `shared/`. Lý do: Flutter app phức tạp hơn (native platform, offline storage).

---

## di.config.dart — DI Container Architecture

> ⚠️ Line numbers trong generated files không ổn định — mỗi lần chạy `build_runner` có thể thay đổi. Search theo **class name / function name** thay vì line number.

Đã đọc `di.dart` ở [M1](../module-01-app-entrypoint/01-code-walk.md#didart--dependency-injection-setup). Giờ xem file **generated** để hiểu DI container đăng ký dependencies thế nào.

<!-- AI_VERIFY: base_flutter/lib/di.config.dart#L52-L68 -->
```dart
    gh.factory<_i887.JsonArrayErrorResponseDecoder>(
        () => _i887.JsonArrayErrorResponseDecoder());
    gh.factory<_i1065.JsonObjectErrorResponseDecoder>(
        () => _i1065.JsonObjectErrorResponseDecoder());
    gh.factory<_i28.LineErrorResponseDecoder>(
        () => _i28.LineErrorResponseDecoder());
    await gh.factoryAsync<_i460.SharedPreferences>(
      () => serviceModule.prefs,
      preResolve: true,
    );
    gh.lazySingleton<_i342.FirebaseFirestoreService>(
        () => _i342.FirebaseFirestoreService());
    gh.lazySingleton<_i623.FirebaseMessagingService>(
        () => _i623.FirebaseMessagingService());
    gh.lazySingleton<_i889.UploadFileServerApiClient>(
        () => _i889.UploadFileServerApiClient());
    gh.lazySingleton<_i666.RawApiClient>(() => _i666.RawApiClient());
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/di.config.dart)

> 🔎 **Quan sát**
> - **3 registration types** xuất hiện:
>   1. `gh.factory<T>()` — tạo instance **mới** mỗi lần `getIt.get<T>()`
>   2. `gh.factoryAsync<T>()` — factory **async** + `preResolve: true` → resolve **trước khi** DI ready
>   3. `gh.lazySingleton<T>()` — tạo instance **1 lần**, lazy (chỉ tạo khi gọi lần đầu)
> - `factory` dùng cho stateless objects (JSON decoders) — tạo mới mỗi lần, không giữ state
> - `lazySingleton` dùng cho services cần **share state** (API clients, helpers, preferences) — 1 instance toàn app
> - `factoryAsync` + `preResolve` dùng cho platform dependencies cần async initialization (SharedPreferences)

---

### DI dependencies with injection

<!-- AI_VERIFY: base_flutter/lib/di.config.dart#L85-L115 -->
```dart
    gh.lazySingleton<_i49.AppPreferences>(
        () => _i49.AppPreferences(gh<_i460.SharedPreferences>()));
    gh.lazySingleton<_i267.LocalPushNotificationHelper>(
        () => _i267.LocalPushNotificationHelper(gh<_i199.PackageHelper>()));
    gh.lazySingleton<_i928.AppNavigator>(
        () => _i928.AppNavigator(gh<_i199.AppRouter>()));
    gh.lazySingleton<_i605.AppApiService>(() => _i605.AppApiService(
          gh<_i199.NoneAuthAppServerApiClient>(),
          gh<_i199.AuthAppServerApiClient>(),
          gh<_i199.UploadFileServerApiClient>(),
        ));
    gh.lazySingleton<_i741.DeepLinkHelper>(() => _i741.DeepLinkHelper(
          gh<_i199.AppNavigator>(),
          gh<_i199.AppPreferences>(),
        ));
    gh.lazySingleton<_i75.AppDatabase>(
        () => _i75.AppDatabase(gh<_i199.AppPreferences>()));
    gh.factory<_i94.RouteGuard>(
        () => _i94.RouteGuard(gh<_i199.AppPreferences>()));
    gh.factory<_i729.GetNotificationsPagingExecutor>(
        () => _i729.GetNotificationsPagingExecutor(gh<_i199.AppApiService>()));
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/di.config.dart)

> 🔎 **Quan sát**
> - **Constructor injection:** `AppPreferences(gh<SharedPreferences>())` — DI tự động truyền dependency vào constructor
> - **Dependency chain:** `AppApiService` cần 3 API clients → DI resolve lần lượt
> - `RouteGuard` dùng `factory` (không phải `lazySingleton`) — mỗi lần cần route guard tạo instance mới → tránh stale state
> - Import qua `_i199` (alias cho `index.dart`) — generated code cũng **import qua barrel file**
> - **Hỏi:** `AppNavigator` phụ thuộc `AppRouter`. Nếu `AppRouter` chưa registered, chuyện gì xảy ra?

> 💡 **FE Perspective**
> **Flutter:** DI container (get_it + injectable) tự resolve dependency tree qua constructor injection — developer chỉ khai báo annotation (`@injectable`, `@lazySingleton`), container lo phần wiring.
> **React/Vue tương đương:** Truyền dependency qua props/context hoặc `new AppApiService(client1, client2)` — manual wiring.
> **Khác biệt quan trọng:** DI container tự động resolve toàn bộ dependency chain, FE phải manual compose hoặc dùng NestJS-style DI cho backend.

---

## my_app.dart — App Widget Root

<!-- AI_VERIFY: base_flutter/lib/ui/my_app.dart#L1-L9 -->
```dart
import 'package:auto_route/auto_route.dart';
import 'package:device_preview_plus/device_preview_plus.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

import '../index.dart';
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/ui/my_app.dart)

> 🔎 **Quan sát**
> - `import '../index.dart'` — import barrel file bằng **relative path** (lên 1 cấp vì file nằm trong `ui/`). Đây là convention bắt buộc ([common_coding_rules.md](../../base_flutter/docs/technical/common_coding_rules.md))
> - **Không import** trực tiếp `app_router.dart`, `config.dart`, `constant.dart` — tất cả qua `index.dart`
> - Package imports (`auto_route`, `device_preview_plus`, `hooks_riverpod`) đặt trước project imports — Dart convention

---

### MyApp class — HookConsumerWidget

<!-- AI_VERIFY: base_flutter/lib/ui/my_app.dart#L10-L20 -->
```dart
class MyApp extends HookConsumerWidget {
  const MyApp({required this.initialResource, super.key});

  final InitialResource initialResource;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final appRouter = ref.watch(appRouterProvider);

    LocaleSettings.setLocaleRawSync('ja');

    return Consumer(
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/ui/my_app.dart)

> 🔎 **Quan sát**
> - `Widget build(BuildContext context, WidgetRef ref)` — method **bắt buộc** của mọi widget. Flutter gọi `build()` mỗi khi cần render/re-render UI, tương tự React `render()` / Vue `<template>`. Nhận `BuildContext` (vị trí trong widget tree) và trả về widget tree con. Với `HookConsumerWidget`, thêm `WidgetRef ref` để truy cập Riverpod providers. Deep dive ở M7.
> - `HookConsumerWidget` — kết hợp Riverpod (`Consumer`) + Flutter Hooks (`Hook`). Đây là class cơ bản cho UI trong project (deep dive ở [M8 — State Management](../module-08-riverpod-state/))
> - `ref.watch(appRouterProvider)` — lấy `AppRouter` từ Riverpod provider (không phải `getIt` — đây là bridge giữa DI và Riverpod)
> - `InitialResource` — data loaded ở boot sequence ([M1 § _loadInitialResource](../module-01-app-entrypoint/01-code-walk.md#_loadinitialresource--load-dữ-liệu-khởi-tạo))
> - `const MyApp(...)` — constructor nhận `initialResource`, widget này **const-eligible**

---

### MaterialApp.router — Routing setup

<!-- AI_VERIFY: base_flutter/lib/ui/my_app.dart#L27-L62 -->
```dart
            builder: (_) => MaterialApp.router(
              builder: (context, child) {
                final widget = MediaQuery.withClampedTextScaling(
                  maxScaleFactor: Constant.appMaxTextScaleFactor,
                  minScaleFactor: Constant.appMinTextScaleFactor,
                  child: child ?? const SizedBox.shrink(),
                );

                return Config.enableDevicePreview
                    ? DevicePreview.appBuilder(context, widget)
                    : widget;
              },
              routerDelegate: appRouter.delegate(
                deepLinkBuilder: (deepLink) {
                  return DeepLink(_mapRouteToPageRouteInfo());
                },
                navigatorObservers: () => [AppNavigatorObserver()],
              ),
              routeInformationParser: appRouter.defaultRouteParser(),
              title: Constant.materialAppTitle,
              color: Constant.taskMenuMaterialAppColor,
              themeMode: ThemeMode.light,
              theme: lightTheme,
              darkTheme: darkTheme,
              debugShowCheckedModeBanner: kDebugMode,
              localeResolutionCallback: (Locale? locale, Iterable<Locale> supportedLocales) =>
                  supportedLocales.map((e) => e.languageCode).contains(locale?.languageCode)
                      ? locale
                      : const Locale('ja'),
              locale:
                  Config.enableDevicePreview ? DevicePreview.locale(context) : const Locale('ja'),
              supportedLocales: AppLocaleUtils.supportedLocales,
              localizationsDelegates: const [
                GlobalMaterialLocalizations.delegate,
                GlobalWidgetsLocalizations.delegate,
                GlobalCupertinoLocalizations.delegate,
              ],
            ),
```
<!-- END_VERIFY -->

→ [Mở file gốc](../../base_flutter/lib/ui/my_app.dart)

> 🔎 **Quan sát**
> - `MaterialApp.router` — variant hỗ trợ **declarative routing** (Router API). Không dùng `MaterialApp()` thông thường
> - `routerDelegate: appRouter.delegate(...)` — quản lý navigation stack và xử lý deep links. `auto_route` tạo delegate từ route config, tương tự React Router `<Routes>` element
> - `routeInformationParser: appRouter.defaultRouteParser()` — parse URL/deep link thành route configuration mà delegate hiểu được. Cặp delegate + parser = Router API của Flutter
> - `Consumer` (ở ngoài `MaterialApp.router`) — Riverpod widget rebuild khi provider thay đổi, đảm bảo `appRouter` luôn up-to-date
> - Deep dive routing ở [M5 — Navigation](../module-05-navigation/)
> - `theme: lightTheme` / `darkTheme: darkTheme` — theme từ `resource/` layer (deep dive ở [M3 — Resource Layer](../module-03-common-layer/))
> - `Constant.*` — app constants từ `common/constant.dart` — config tập trung, không hardcode
> - `Config.enableDevicePreview` — feature flag từ `common/config.dart` — toggle DevicePreview tool
> - **Hỏi:** Tại sao `lightTheme` và `darkTheme` không prefix `AppThemes.`? (Gợi ý: barrel file export)

> 💡 **FE Perspective**
> **Flutter:** `MaterialApp.router` gom routing, theme, localization vào **1 widget** duy nhất — cấu hình tập trung, ít boilerplate.
> **React/Vue tương đương:** `BrowserRouter` (routing) + `ThemeProvider` (theme) + `IntlProvider` (i18n) — phải compose nhiều providers lồng nhau.
> **Khác biệt quan trọng:** Flutter cấu hình tất cả ở 1 chỗ (`MaterialApp`), React/Vue phải tự tổ chức provider hierarchy, dễ gây "provider hell".

---

## 📊 Code Walk Summary

| File | Lines | Vai trò | Generated? | Liên kết |
|------|-------|---------|------------|----------|
| `index.dart` | 162 | Barrel export toàn bộ `lib/` | ✅ `make ep` | [source](../../base_flutter/lib/index.dart) |
| `di.config.dart` | 126 | DI container registrations | ✅ `make fb` | [source](../../base_flutter/lib/di.config.dart) |
| `my_app.dart` | 80 | App widget root, routing + theme | ❌ Manual | [source](../../base_flutter/lib/ui/my_app.dart) |
| `di.dart` | 16 | DI setup (annotation source) | ❌ Manual | [source](../../base_flutter/lib/di.dart) |

**Pattern chính:**
1. **Barrel file** — 1 `index.dart` export toàn bộ, auto-generated, luôn import qua đây
2. **Layer architecture** — 7 layers rõ ràng: common → data_source → exception → model → navigation → resource → ui
3. **DI registration types** — `factory` (stateless), `lazySingleton` (shared state), `factoryAsync` (async init)
4. **Generated vs manual** — `index.dart` + `di.config.dart` = generated; `di.dart` + `my_app.dart` = manual. Rule: **không sửa file generated**

**Lưu ý:** Tất cả rules chi tiết về import/export → xem [common_coding_rules.md](../../base_flutter/docs/technical/common_coding_rules.md). Naming conventions → xem [naming_rules.md](../../base_flutter/docs/technical/naming_rules.md).

---

**Tiếp theo:** [02-concept.md](./02-concept.md) — 6 concepts rút ra từ code walk.

<!-- AI_VERIFY: generation-complete -->
